# Onboarding: apps/hub

This app now lives inside the `ericgitonga-com` monorepo (migrated from the standalone
`eric-gitonga-links` repo, now archived). The workflow conventions — issue-first, branch+PR,
CI/merge/versioning — are shared across the whole monorepo: see the repo root's `ONBOARDING.md`,
not a copy here.

See this directory's own `README.md` for what this app is and how it's structured.
